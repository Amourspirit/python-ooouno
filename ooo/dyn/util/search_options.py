# coding: utf-8
#
# Copyright 2022 :Barry-Thomas-Paul: Moss
#
# Licensed under the Apache License, Version 2.0 (the "License")
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http: // www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# Struct Class
# this is a auto generated file generated by Cheetah
# Namespace: com.sun.star.util
# Libre Office Version: 7.2
from typing import TYPE_CHECKING
from ooo.oenv import UNO_ENVIRONMENT, UNO_RUNTIME, UNO_NONE
_DYNAMIC = False
if (not TYPE_CHECKING) and UNO_RUNTIME and UNO_ENVIRONMENT:
    _DYNAMIC = True

if not TYPE_CHECKING and _DYNAMIC:
    def _dynamic_struct():
        import uno
        from com.sun.star.util import SearchOptions as USearchOptions
        # Dynamically create uno com.sun.star.util.SearchOptions using uno
        global SearchOptions

        def _set_fn_attr(struct):
            type_name = 'com.sun.star.util.SearchOptions'
            struct.__dict__['typeName'] = type_name
            struct.__dict__['__pyunointerface__'] = type_name
            struct.__dict__['__pyunostruct__'] = type_name

        def _set_attr(struct):
            struct.__dict__['__ooo_ns__'] = 'com.sun.star.util'
            struct.__dict__['__ooo_full_ns__'] = 'com.sun.star.util.SearchOptions'
            struct.__dict__['__ooo_type_name__'] = 'struct'

        def _struct_init(algorithmType = UNO_NONE, searchFlag = UNO_NONE, searchString = UNO_NONE, replaceString = UNO_NONE, Locale = UNO_NONE, changedChars = UNO_NONE, deletedChars = UNO_NONE, insertedChars = UNO_NONE, transliterateFlags = UNO_NONE):
            ns = 'com.sun.star.util.SearchOptions'
            if isinstance(algorithmType, USearchOptions):
                inst = uno.createUnoStruct(ns, algorithmType)
                _set_attr(inst)
                return inst
            struct = uno.createUnoStruct(ns)

            if not algorithmType is UNO_NONE:
                if getattr(struct, 'algorithmType') != algorithmType:
                    setattr(struct, 'algorithmType', algorithmType)
            if not searchFlag is UNO_NONE:
                if getattr(struct, 'searchFlag') != searchFlag:
                    setattr(struct, 'searchFlag', searchFlag)
            if not searchString is UNO_NONE:
                if getattr(struct, 'searchString') != searchString:
                    setattr(struct, 'searchString', searchString)
            if not replaceString is UNO_NONE:
                if getattr(struct, 'replaceString') != replaceString:
                    setattr(struct, 'replaceString', replaceString)
            if not Locale is UNO_NONE:
                if getattr(struct, 'Locale') != Locale:
                    setattr(struct, 'Locale', Locale)
            if not changedChars is UNO_NONE:
                if getattr(struct, 'changedChars') != changedChars:
                    setattr(struct, 'changedChars', changedChars)
            if not deletedChars is UNO_NONE:
                if getattr(struct, 'deletedChars') != deletedChars:
                    setattr(struct, 'deletedChars', deletedChars)
            if not insertedChars is UNO_NONE:
                if getattr(struct, 'insertedChars') != insertedChars:
                    setattr(struct, 'insertedChars', insertedChars)
            if not transliterateFlags is UNO_NONE:
                if getattr(struct, 'transliterateFlags') != transliterateFlags:
                    setattr(struct, 'transliterateFlags', transliterateFlags)
            _set_attr(struct)
            return struct
        _set_attr(_struct_init)
        _set_fn_attr(_struct_init)
        SearchOptions = _struct_init

    _dynamic_struct()
else:
    from ...lo.util.search_options import SearchOptions as SearchOptions

__all__ = ['SearchOptions']

