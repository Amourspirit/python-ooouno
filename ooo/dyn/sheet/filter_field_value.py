# coding: utf-8
#
# Copyright 2022 :Barry-Thomas-Paul: Moss
#
# Licensed under the Apache License, Version 2.0 (the "License")
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http: // www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# Struct Class
# this is a auto generated file generated by Cheetah
# Namespace: com.sun.star.sheet
# Libre Office Version: 7.2
from typing import TYPE_CHECKING
from ooo.oenv import UNO_ENVIRONMENT, UNO_RUNTIME, UNO_NONE
_DYNAMIC = False
if (not TYPE_CHECKING) and UNO_RUNTIME and UNO_ENVIRONMENT:
    _DYNAMIC = True

if not TYPE_CHECKING and _DYNAMIC:
    def _dynamic_struct():
        import uno
        from com.sun.star.sheet import FilterFieldValue as UFilterFieldValue
        # Dynamically create uno com.sun.star.sheet.FilterFieldValue using uno
        global FilterFieldValue

        def _set_attr(struct):
            struct.__dict__['__ooo_ns__'] = 'com.sun.star.sheet'
            struct.__dict__['__ooo_full_ns__'] = 'com.sun.star.sheet.FilterFieldValue'
            struct.__dict__['__ooo_type_name__'] = 'struct'

        def _struct_init(IsNumeric = UNO_NONE, NumericValue = UNO_NONE, StringValue = UNO_NONE, FilterType = UNO_NONE, ColorValue = UNO_NONE):
            ns = 'com.sun.star.sheet.FilterFieldValue'
            if isinstance(IsNumeric, UFilterFieldValue):
                inst = uno.createUnoStruct(ns, IsNumeric)
                _set_attr(inst)
                return inst
            struct = uno.createUnoStruct(ns)

            if not IsNumeric is UNO_NONE:
                if getattr(struct, 'IsNumeric') != IsNumeric:
                    setattr(struct, 'IsNumeric', IsNumeric)
            if not NumericValue is UNO_NONE:
                if getattr(struct, 'NumericValue') != NumericValue:
                    setattr(struct, 'NumericValue', NumericValue)
            if not StringValue is UNO_NONE:
                if getattr(struct, 'StringValue') != StringValue:
                    setattr(struct, 'StringValue', StringValue)
            if not FilterType is UNO_NONE:
                if getattr(struct, 'FilterType') != FilterType:
                    setattr(struct, 'FilterType', FilterType)
            if not ColorValue is UNO_NONE:
                if getattr(struct, 'ColorValue') != ColorValue:
                    setattr(struct, 'ColorValue', ColorValue)
            _set_attr(struct)
            return struct
        FilterFieldValue = _struct_init

    _dynamic_struct()
else:
    from ...lo.sheet.filter_field_value import FilterFieldValue as FilterFieldValue

__all__ = ['FilterFieldValue']

